#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int min;
int visited[15][15] = {0};
int map[15][15] = {0};

int main(void) {
    int i,j,n;
    int temp;

    scanf("%d", &n);
    
    for(i = 0; i < n; ++i) {
        for(j = 0; j < n; ++j) {
            scanf("%d",&temp);
        }

    }
}