#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;

int N, M, start, goal;
vector<bool> visited;
vector<vector<int>> nexts,costs;
vector<int> takeCosts;
queue<int> que;

void init() {
    int i, s,e,c;
    cin >> N >> M;


    for(i = 0; i < M; ++i) {
        cin >> s >> e >> c;
        
    }

}

int main() {
    
}